import React from 'react'
import ReactDOM from 'react-dom'
import ReactRouterSetup from './App'

ReactDOM.render(
  <React.StrictMode>
    <ReactRouterSetup />
  </React.StrictMode>,
  document.getElementById('root')
)
